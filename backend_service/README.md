# Red Sea Express — Backend Service (placeholder)

This directory is reserved for the backend runtime (future implementation).
It is intentionally empty in Phase A except for scaffolding files.

What is included now (Phase A):
- README.md (this file)
- src/ (placeholder directory)
- tests/ (placeholder directory)

Purpose
The backend_service folder will contain the backend application (NestJS/Express) source, Dockerfile, and any runtime configuration in Phase B.

Do NOT add production code here until Phase B starts. For now this folder only indicates where the backend runtime will live.

Quick notes for Phase B:
- Add a Dockerfile to build the backend container.
- Add a package.json and the source entrypoint (e.g., src/main.ts or src/main.js).
- Add Prisma schema and migrations under prisma/ if using Prisma.
